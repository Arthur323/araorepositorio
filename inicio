#include <stdlib.h>
#include <stdio.h>
int main (){
int a;
scanf(“%d”, &a);
// Em C temos estruturas de decisão que geram um fluxo para o nosso programa
// O if é basicamente uma verificação de condição que compila um bloco de código caso seja verdadeira
// Se a condição for falsa, o bloco é ignorado
// Sintaxe: if <condição> {<bloco de código>}
// Para se fazer a verificação, são usados operadores de comparação, que são:
// ‘==’ -> Igual | ‘>’ -> Maior que | ‘>=’ -> Maior ou Igual a | ‘<‘ -> Menor que | ‘<=’ -> Menor ou igual a
// ‘!=’ -> Diferente de
if(a == 1){
printf(“\nA variavel a e’ igual a 1”);
}
if(a != 1){
printf(“\nA variavel a e’ diferente de 1”);
}
if(a > 1){
printf(“\nA variavel a e’ maior que 1”);
}
if(a >= 1){
printf(“\nA variavel a e’ maior ou igual a 1”);
}
if(a < 1){
printf(“\nA variavel a e’ menor que 1”);
}
if(a <= 1){
printf(“\nA variavel a e’ menor ou igual a 1”);
}
return 0;
}
